services:
  postgres:
    image: postgres:17.6
    container_name: my_postgres
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: devuser
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: devdb
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:8.2.1
    container_name: my_redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

  redis-rest:
    build:
      context: ./docker/redis-rest
    container_name: my_redis_rest
    environment:
      REDIS_URL: redis://redis:6379
      UPSTASH_REDIS_REST_TOKEN: ${UPSTASH_REDIS_REST_TOKEN:-dev-token}
      UPSTASH_REDIS_REST_PORT: ${UPSTASH_REDIS_REST_PORT:-8079}
    ports:
      - "${UPSTASH_REDIS_REST_PORT:-8079}:${UPSTASH_REDIS_REST_PORT:-8079}"
    depends_on:
      - redis
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
